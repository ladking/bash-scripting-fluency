### Overview ###

This is a simple bash script that displays simple information (os distribution, uptime in days) about the host system. The script allow usage of simple bash command for text processing such as `grep` to search for words that match given pattern of exact, `cut` used to remove characters from a given line, `tr` used to trim lines.
